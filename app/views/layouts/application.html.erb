<!DOCTYPE html>
<html>
  <head>
    <title>ZeroToHero</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>
    <p style="color: green" class="notice"><%= notice %></p>
    <p style="color: red" class="alert"><%= alert %></p>

    <% if user_signed_in? %>
      <strong style="color: blue"><%= current_user.email.split('@').first.capitalize %></strong></br></br>
      <%= link_to "Sign out", destroy_user_session_path, data: { turbo_method: :delete } %></br></br>
      <%= link_to 'Account Settings', edit_user_registration_path %>
    <% else %>
      <%= link_to 'Sign in', new_user_session_path %>
      <%= link_to 'Sign up', new_user_registration_path %>
    <% end %>

    <%= yield %>
  </body>
</html>
