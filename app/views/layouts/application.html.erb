<!DOCTYPE html>
<html>
  <head>
    <title>ZeroToHero</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>

    <%= javascript_include_tag "application", "data-turbo-track": "reload", type: "module" %>
  </head>

  <%= render "layouts/header" %>

  <body class="main">

    <!-- Text using bootstrap -->
    <!-- <button type="button" class="btn btn-primary">Primary</button>
    <button type="button" class="btn btn-secondary">Secondary</button>
    <button type="button" class="btn btn-success">Success</button>
    <button type="button" class="btn btn-danger">Danger</button>
    <button type="button" class="btn btn-warning">Warning</button>
    <button type="button" class="btn btn-info">Info</button>
    <button type="button" class="btn btn-light">Light</button>
    <button type="button" class="btn btn-dark">Dark</button>
    <button type="button" class="btn btn-link">Link</button> -->

    <!-- Test for font awesome -->
    <i class="far fa-address-book"></i>
    <i class="fab fa-github"></i>

    <p style="color: green"><%= notice %></p>
    <p style="color: yellow"><%= alert %></p>
    <p style="color: red"><%= flash[:error] %></p>


    <strong><%= link_to root_path do %>
      <i class="far fa-gem fa-spin fa-2x"></i>
      HOME
    <% end %></strong>
    <strong><%= link_to 'TELEGRAM', telegrams_path %></strong>
    <strong><%= link_to 'ARTICLES', articles_path %></strong><br>
    <hr>
    <br>
    <% if user_signed_in? %>
      <strong style="color: blue">Hello <%= current_user.email.split('@').first.capitalize %></strong>
      <br>

      <%= current_user.nickname %>
      <br>
      <%= resize_image_tag(current_user.image_url, 100, 100)  if current_user.image_url? %>
      <br>
      <%= current_user.uid %>
      <br>
      <%= current_user.provider %>
      <br>

      <%= link_to "Sign out", destroy_user_session_path, data: { turbo_method: :delete } %>
      <br>
      <%= link_to 'Account Settings', edit_user_registration_path %>
    <% else %>
      <%= link_to 'Sign in', new_user_session_path %>
      <br>
      <%= link_to 'Sign up', new_user_registration_path %>
    <% end %>
    <hr>
    <br>
    <%= yield %>
  </body>

  <%= render "layouts/footer" %>

</html>

<style>
  .file-container {
  display: flex;
  flex-direction: row;
  justify-content: center;
}

  .file-thumbnail {
    width: fit-content; /* Độ rộng cố định cho mỗi phần tử */
    margin-right: 20px; /* Khoảng cách giữa các phần tử */
  }

  .main {
    padding: 30px;
  }
  .main strong {
    display: inline-flex; /* hoặc inline-block */
    margin-right: 20px;
  }
  .main strong:last-child {
    margin-right: 0;
  }

</style>
